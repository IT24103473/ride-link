package lk.ridelink.ride.domain;

/** Areas RideLink operates in. Matching never crosses an area boundary. */
public enum ServiceArea {
    COLOMBO,
    NEGOMBO,
    KANDY,
    GALLE,
    TRINCOMALEE
}
